<h1>This is the landing page</h1>


<% if user_signed_in? %>
  <h1> Signed in as: <%= @user.first_name %> <%= @user.last_name %></h1>
  <%= link_to "Continue to User Profile", user_path(@user) %>
<% else %>
    <h1>Please Sign Up or Sign In to Continue!</h1>
<% end %>
